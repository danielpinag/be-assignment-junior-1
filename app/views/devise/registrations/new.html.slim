h1.h3.mb-3.fw-normal Please sign up
= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { autocomplete: "off" }) do |f|
  .form-group.mb-4
    .form-floating
      = f.input :email, input_html: { autocomplete: 'off', class: 'form-control', id: 'floatingInput' }, label: false, wrapper: false
      label for="floatingInput" Email address
    .form-floating
      = f.input :password, input_html: { autocomplete: 'off', class: 'form-control', id: 'floatingPassword' }, label: false, wrapper: false
      label for="floatingPassword" Password
    .form-floating
      = f.input :password_confirmation, input_html: { autocomplete: 'off', class: 'form-control', id: 'floatingPasswordConfirm' }, label: false, wrapper: false
      label for="floatingPasswordConfirm" Password Confirmation
    .form-floating
      = f.input :name, input_html: { autocomplete: 'off', class: 'form-control', id: 'floatingName' }, label: false, wrapper: false
      label for="floatingName" Name
    .form-floating
      = f.input :mobile_number, input_html: { autocomplete: 'off', class: 'form-control', id: 'floatingMobileNumber' }, label: false, wrapper: false
      label for="floatingMobileNumber" Mobile Number

    = f.button :button, "Sign up", class: "w-100 btn btn-lg btn-primary", data: { button_name: "Log in" }


= render "devise/shared/links"